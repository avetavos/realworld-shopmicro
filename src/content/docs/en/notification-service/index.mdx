---
title: Notification Service
description: "React to domain events and notify customers from a standalone Notification consumer."
sidebar:
  order: 0
---

React to domain events and notify customers from a standalone Notification consumer, decoupled from the services that produce those events.

Start with [Consuming events →](/shopmicro/en/notification-service/consume-events/), then work through the lessons in order (see the sidebar).
