---
title: Order Service
description: "Build the Order gRPC server that places orders, writes to PostgreSQL, and emits domain events."
sidebar:
  order: 0
---

Build the Order gRPC server that places orders, persists them to PostgreSQL, and emits the domain events the rest of the system reacts to.

Start with [The gRPC server →](/shopmicro/en/order-service/grpc-server/), then work through the lessons in order (see the sidebar).
