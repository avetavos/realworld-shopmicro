---
title: Catalog Service
description: "Build the Catalog gRPC server, its PostgreSQL store, and the product and inventory endpoints."
sidebar:
  order: 0
---

Build the Catalog gRPC server, its own PostgreSQL store, and the product and inventory endpoints the rest of the system reads from.

Start with [The gRPC server →](/shopmicro/en/catalog-service/grpc-server/), then work through the lessons in order (see the sidebar).
