---
title: API Gateway
description: "Expose the microservices as REST through grpc-gateway behind a single API entry point."
sidebar:
  order: 0
---

Expose the microservices as REST through grpc-gateway, giving clients a single API entry point in front of the gRPC backends.

Start with [grpc-gateway →](/shopmicro/en/gateway/grpc-gateway/), then work through the lessons in order (see the sidebar).
