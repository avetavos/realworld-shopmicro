---
title: Resilience
description: "Add timeouts, retries, backoff, and graceful degradation so the system survives partial failure."
sidebar:
  order: 0
---

Add timeouts, retries, backoff, and graceful degradation so the system keeps working when a single service or dependency fails.

Start with [Timeouts & retries →](/shopmicro/en/resilience/timeouts-retries/), then work through the lessons in order (see the sidebar).
